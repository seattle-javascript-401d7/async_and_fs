const mocha = require("mocha");
const chai = require("chai").expect;
const async = require(__dirname + "/lib/async");

describe("async", function () {
  it("should return 8 bytes of hex", function (done) {
    //none of this shit works
    var printArray;
    done();
  });
});
