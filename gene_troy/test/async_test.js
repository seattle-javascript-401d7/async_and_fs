/*eslint-env mocha */

var async = require(__dirname + '/../async');
var expect = require('chai').expect;

describe('async', () => {

  it('should output the files in order even with one absurdly large first file', (done) => {

        expect(err).to.eql(null);
        expect(true).to.eql(true);
        done();
      );
  });
});
